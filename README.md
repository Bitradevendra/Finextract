# Finextract

`Finextract` currently contains a packaged Windows installer for the app.

## Install

1. Download or clone this repository.
2. Run `FinExtract Pro Setup 1.0.0.exe`.
3. Follow the installer steps.

## Use

Launch the installed application from Windows after setup completes.

## How It Works

This repository stores the installer artifact rather than the raw application source.
